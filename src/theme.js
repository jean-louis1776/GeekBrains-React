import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
    palette: {
        type: 'dark',
        primary: {
            main: '#26a69a',
        },
        secondary: {
            main: '#26a69a',
        },
    },
});

export default theme;