import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#00897b',
        },
        secondary: {
            main: '#00897b',
        },
    },
});

export default theme;